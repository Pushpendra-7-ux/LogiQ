version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: logiq-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/logiq.db:/app/logiq.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
